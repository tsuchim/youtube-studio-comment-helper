name: (disabled) Build and Release

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled. Use release.yml instead.
        run: echo "This workflow is deprecated. Use .github/workflows/release.yml"
