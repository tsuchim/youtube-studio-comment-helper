{
  "name": "youtube-studio-comment-helper",
  "version": "0.2.0",
  "description": "Clean YouTube Studio Comment Helper - handle to display name replacement",
  "main": "background-v2.js",
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix",
    "validate": "node scripts/validate-manifest.js",
    "build": "npm run lint && npm run validate",
    "build:zip": "powershell New-Item -ItemType Directory -Force -Path dist; Compress-Archive -Path manifest.json,background.js,src/,assets/,scripts/,pr-test.js -DestinationPath dist/youtube-studio-comment-helper.zip -Force",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "keywords": ["youtube", "chrome-extension", "comment-helper"],
  "author": "Your Name",
  "license": "MIT"
}
