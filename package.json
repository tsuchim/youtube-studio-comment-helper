{
  "name": "youtube-studio-comment-helper",
  "version": "0.2.3",
  "description": "Clean YouTube Studio Comment Helper - handle to display name replacement",
  "main": "background-v2.js",
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix",
    "validate": "node scripts/validate-manifest.js",
    "build": "npm run lint && npm run validate && node scripts/check-version-tag.js",
    "build:zip": "npm run version:sync && node scripts/check-version-tag.js && npm run build && node scripts/build-zip.js",
    "version:sync": "node scripts/sync-manifest-version.js",
    "release": "bash scripts/release.sh",
    "release:tag": "bash scripts/release-tag.sh",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "archiver": "^5.3.2",
    "standard-version": "^9.5.0"
  },
  "keywords": [
    "youtube",
    "chrome-extension",
    "comment-helper"
  ],
  "author": "Yuji Tsuchimoto <info-chrome@eikai.co.jp>",
  "license": "AGPL-3.0"
}
